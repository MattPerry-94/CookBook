<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inscription - CookBook</title>
    <link rel="stylesheet" href="/CookBook/public/css/style.css?v=4">
</head>
<body>
<div class="auth-page">
    <div class="auth-card">
        <h1>Inscription</h1>
        <p class="auth-subtitle">Créez votre compte pour gérer vos recettes.</p>

        {% if error is defined %}
            <div class="error" style="color: red; margin-bottom: 1rem;">{{ error }}</div>
        {% endif %}

        <form method="POST" action="/CookBook/signup">
            <div>
                <label for="username">Nom d'utilisateur</label>
                <input placeholder="Votre pseudo" type="text" id="username" name="username" value="{{ old_username|default('') }}" required />
            </div>

            <div>
                <label for="email">Votre email</label>
                <input placeholder="nom@fournisseur.ext" type="email" id="email" name="email" value="{{ old_email|default('') }}" required />
            </div>

            <div>
                <label for="password">Votre mot de passe</label>
                <input placeholder="votre mot de passe" type="password" id="password" name="password" required />
            </div>

            <div class="auth-actions">
                <button type="submit">S'inscrire</button>
                <p class="auth-link">
                    Vous avez déjà un compte ?
                    <a href="/CookBook/signin">Connectez-vous</a>
                </p>
            </div>
        </form>
    </div>
</div>
</body>
</html>
