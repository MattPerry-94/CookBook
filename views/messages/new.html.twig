<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nouvelle conversation - CookBook</title>
    <link rel="stylesheet" href="/CookBook/public/css/style.css">
</head>
<body>
<header>
    <div>
        <strong>CookBook</strong>
    </div>
    <nav>
        <a href="/CookBook/">Accueil</a>
        {% if currentUser %}
            <a href="/CookBook/my-recipes">Mes recettes</a>
            <a href="/CookBook/messages" class="active">
                Messages
                {% if unreadMessagesCount > 0 %}
                    <span class="badge" style="background: red; color: white; border-radius: 50%; padding: 2px 6px; font-size: 0.8rem; vertical-align: top;">{{ unreadMessagesCount }}</span>
                {% endif %}
            </a>
            <a href="/CookBook/recipes/create">Créer une recette</a>
            {% if isAdmin %}
                <a href="/CookBook/admin">Administration</a>
            {% endif %}
            <a href="/CookBook/logout">Déconnexion</a>
            <span class="user-badge">{{ currentUser }}</span>
        {% else %}
            <a href="/CookBook/signin">Connexion</a>
        {% endif %}
    </nav>
</header>

<main>
    <div style="margin-bottom: 1rem;">
        <a href="/CookBook/messages">&larr; Retour aux conversations</a>
    </div>

    <h1>Démarrer une conversation</h1>
    <p>Choisissez un utilisateur pour lui envoyer un message :</p>

    {% if users is defined and users|length > 0 %}
        <div class="users-list" style="display: grid; gap: 1rem; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));">
            {% for user in users %}
                <div class="card" style="padding: 1rem; display: flex; flex-direction: column; gap: 0.5rem;">
                    <h3>{{ user.name|default('Utilisateur sans nom') }}</h3>
                    <small>{{ user.email }}</small>
                    <a href="/CookBook/messages/{{ user.id }}" class="button" style="text-align: center; background: #ff7043; color: white; padding: 0.5rem; border-radius: 4px; text-decoration: none; margin-top: auto;">Envoyer un message</a>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <p>Aucun autre utilisateur trouvé.</p>
    {% endif %}
</main>
</body>
</html>
